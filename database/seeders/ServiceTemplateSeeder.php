<?php

namespace Database\Seeders;

use App\Models\Category;
use Illuminate\Database\Seeder;

class ServiceTemplateSeeder extends Seeder
{
    /**
     * Tabela não existente, mas usaremos a lógica se necessária no futuro.
     * Este seeder é um placeholder documentado até criarmos a tabela 'service_templates'.
     * Run the database seeds.
     */
    public function run(): void
    {
        // $catStreaming = Category::where('slug', 'streaming')->first();
        // Templates de Netflix, Spotify e mais injetados aqui para a base read-only.
    }
}
